sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t wangbaoming/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true wangbaoming/react-test npm test

after_success:
  - docker build -t wangbaoming/multi-client ./client
  - docker build -t wangbaoming/multi-nginx ./nginx
  - docker build -t wangbaoming/multi-server ./server
  - docker build -t wangbaoming/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push wangbaoming/multi-client
  - docker push wangbaoming/multi-nginx
  - docker push wangbaoming/multi-server
  - docker push wangbaoming/multi-worker
